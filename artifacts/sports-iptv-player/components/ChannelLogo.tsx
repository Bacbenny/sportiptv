import { StyleSheet, Text, View } from 'react-native';
import type { Channel } from '@/data/channels';
type Props = { channel: Channel; size?: number };
export function ChannelLogo({ channel, size = 52 }: Props) { return <View style={[styles.logo, { width: size, height: size, borderRadius: size * 0.3, backgroundColor: channel.logoColor }]}><View style={[styles.orbit, { borderColor: channel.accent, width: size * 0.66, height: size * 0.66, borderRadius: size }]} /><Text style={[styles.letters, { color: channel.accent, fontSize: size * 0.23 }]}>{channel.shortName}</Text><View style={[styles.dot, { backgroundColor: '#F7F9FC', width: size * 0.08, height: size * 0.08, borderRadius: size }]} /></View>; }
const styles = StyleSheet.create({ logo: { alignItems: 'center', justifyContent: 'center', overflow: 'hidden' }, orbit: { position: 'absolute', borderWidth: 1, transform: [{ rotate: '-22deg' }] }, letters: { fontFamily: 'Inter_700Bold', letterSpacing: 0.4 }, dot: { position: 'absolute', top: '19%', right: '20%' } });
